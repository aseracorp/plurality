# 2
- let app select server URL !
- tool to request extenal AI (ex. promote a request or something) - keep cost down
- Per-user everything: using a map of linux user > plurality user 
  - Per-user MCP
  - docker ?
- Agents (sub conversations as conversatiosn)
  - select sub of tools + models for each 
  - add message to another chat (agent restarting another chat)
- Notifications
- Client lock (if client tools used and such for remote )
- Preset can set shortcut (fast/medium/smart)
- UI to manage presets / shortcuts?
- Webhooks

#3
- Add a way for AI to include pre-made prompt in the message (like "click here to know more" > paste the prompt "tell me more about X")
- forced tool : calculator
- chart? Mermaid? 
- Trim conversation to avoid errorrs (max token size)
- Trim tool calls to avoid pulution on playwright for ex.
- let "web visit" save to file 
- Knowledge base
- "always allow this tool"
- attachment cleanup ?
- transfer client / server as file tool
- code tools
- client side command 
- Shared memory "skill"
- background process tool (for commands like web servers) (simple detached flag, maybe some log tail function ?)
- browser tools 
- self improving system prompts
- when error move prompt back to input 
- stop should refresh from DB
- Redesign the app using an oled black design (or white in light mode) and the gradient (238, 70, 84) > (245, 194, 86) in a subtle but relevant manner (ex. buttons and stuff but not entire messages) also the Title instead of a "Start a new Conversation" should be "Plurality AI" with Plurality in white, and AI using the gradient described earlier
- WHY ARE TOOLS HARDCODED IN THE API_models.go file ???
- tables look aweful on mobile
- Test client tools 
  - what happens if 2+ clients opened? 
- When status says "open conversations has an udpate" (from a different client potentially) refresh the conversation ? (without over refreshing if current client)
- notifs? 
- client to compress image for vision?
- eco mode 
